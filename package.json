{
  "name": "poe2-waystone-overlay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "verify-adapter": "esbuild src/analyzer/adapter.ts --bundle --platform=node --format=esm --outfile=scripts/.adapter-bundle.mjs && node scripts/verify-adapter.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2",
    "@tauri-apps/plugin-fs": "^2.5.1",
    "@tauri-apps/plugin-global-shortcut": "^2.3.2",
    "@tauri-apps/plugin-notification": "^2.3.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "~5.6.3",
    "vite": "^6.3.5"
  }
}
